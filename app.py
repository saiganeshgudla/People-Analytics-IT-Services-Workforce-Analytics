# app.py
import streamlit as st

def main():
    st.set_page_config(page_title="PeopleLens", page_icon="📊", layout="wide")
    st.title("📊 PeopleLens Workforce Analytics Platform")
    st.subheader("NimbusTech Employee Career & Attrition Simulation Dashboard")
    st.write("This dashboard is currently under construction. Run `python main.py` to generate the dataset first.")

if __name__ == '__main__':
    main()
